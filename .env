TL_API_KEY=tlk_0V8JMAS33ECG8F2TDH2Z13970Q0Q
TL_INDEX_ID=691db483ca1c45a312c68ec0
TL_LIVE_ENABLED=true

# 搜索模态配置（Search Options）
# 核心概念：
# - model_options（索引创建时）：决定哪些模态被索引，创建后不可修改
# - search_options（搜索时）：决定使用哪些模态搜索，必须是 model_options 的子集
# 重要：启用前请确认索引支持，使用 `python scripts/dev/verify_config.py` 验证
TL_AUDIO_SEARCH_ENABLED=true
TL_TRANSCRIPTION_SEARCH_ENABLED=false

# 高级搜索选项
TL_TRANSCRIPTION_MODE=semantic  # 仅当使用 transcription 模态时有效（需 Marengo 3.0）
TL_SEARCH_OPERATOR=or           # 多模态组合方式
TL_CONFIDENCE_THRESHOLD=0.0     # 置信度阈值

# AI 查询改写配置
DEEPSEEK_API_KEY=sk-703f6fa9a284428e865cc8c59332ba87
QUERY_REWRITE_ENABLED=true
QUERY_REWRITE_MANDATORY=false    # 按需改写模式：仅在原始查询无结果时才改写

# 数据库和队列
ENABLE_ASYNC_QUEUE=false         # 本地调试先同步执行
POSTGRES_DSN=sqlite+aiosqlite:///./dev.db   # 或真实数据库 DSN
REDIS_URL=redis://localhost:6379/0

# 媒资配置
VIDEO_ASSET_DIR=media/video
AUDIO_ASSET_DIR=media/audio
MEDIA_BUCKET=lyrics-mix-media
MINIO_ENDPOINT=http://localhost:9000   # 若没有 MinIO，可保持这个值，渲染输出会落在本地临时目录
FALLBACK_VIDEO_ID=6911acda8bf751b791733149
WHISPER_MODEL_NAME=large-v3
